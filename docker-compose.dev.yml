services:
  setting-service:
    extends:
      file: docker-compose.base.yml
      service: setting-service
    build:
      context: "."
      dockerfile: "./Dockerfile.dev"
    restart: "always"
    volumes:
      - ".:/app"
      - "/app/.venv"
